{
  "_conn_section": {
    "description": "=== 连接 ===",
    "type": "text",
    "invisible": true
  },
  "comfyui_host": {
    "type": "string",
    "default": "127.0.0.1",
    "hint": "ComfyUI 主机 IP（默认本机 127.0.0.1；局域网/ZeroTier 填对应地址）",
    "description": "ComfyUI 服务地址 IP，可按实际环境自填"
  },
  "comfyui_port": {
    "type": "int",
    "default": 8188,
    "hint": "ComfyUI 服务端口，默认 8188"
  },
  "comfyui_timeout": {
    "type": "int",
    "default": 300,
    "slider": {
      "min": 60,
      "max": 1800,
      "step": 30
    },
    "hint": "单次生成等待上限（秒）。大分辨率/多 LoRA 建议不低于 300；链路经 ZeroTier 等中转时适当调大。超时后任务仍在 ComfyUI 队列里，可用 comfyui_job 查询。"
  },
  "model_cache_ttl": {
    "type": "int",
    "default": 600,
    "slider": {
      "min": 0,
      "max": 3600,
      "step": 60
    },
    "hint": "模型/LoRA 清单缓存刷新间隔（秒）。0 表示每次查询都强制同步（ComfyUI 端会多一次请求）；装了 LoRA Manager 时分类标签随缓存一并刷新。"
  },
  "lora_manager_enabled": {
    "type": "bool",
    "default": true,
    "hint": "复用 ComfyUI LoRA Manager 的分类、标签、用途说明、推荐权重和触发词；未安装时自动跳过"
  },
  "_family_section": {
    "description": "=== 生图、编辑图与配方 ===",
    "type": "text",
    "invisible": true
  },
  "model_families": {
    "type": "template_list",
    "description": "生图家族与工作流",
    "hint": "先在 Workflow Studio 工作流页导入生图工作流，再给每个家族选择工作流。家族名是暴露给 LLM 的稳定路由名；修改后重载插件生效。",
    "obvious_hint": true,
    "templates": {
      "family": {
        "name": "生图家族",
        "items": {
          "name": {
            "type": "string",
            "description": "生图家族名",
            "default": "",
            "hint": "暴露给 LLM 的稳定名称，例如 anima、krea2、flux；须唯一，重复时保留第一条"
          },
          "workflow": {
            "type": "string",
            "description": "生图工作流",
            "default": "",
            "hint": "从插件数据目录已导入的工作流中选择；多个家族可以共用一张工作流。导入新工作流后重新打开此配置页"
          },
          "edit_workflow": {
            "type": "string",
            "default": "",
            "invisible": true,
            "hint": "旧配置兼容；新配置请使用独立编辑工作流路由"
          },
          "prompt_style": {
            "type": "string",
            "default": "auto",
            "options": [
              "auto",
              "danbooru",
              "natural"
            ],
            "labels": [
              "自动判断",
              "Danbooru 标签串",
              "自然语言"
            ],
            "hint": "提示 LLM 用哪种提示词风格：anima/illustrious 系选标签串，krea/flux/qwen 系选自然语言，不确定选自动判断"
          },
          "description": {
            "type": "string",
            "default": "",
            "hint": "简短说明适合的画面或用途，会随家族名一起暴露给 LLM，帮它选对家族"
          }
        }
      }
    },
    "default": [
      {
        "__template_key": "family",
        "name": "anima",
        "workflow": "anima-v3",
        "prompt_style": "danbooru",
        "description": "动漫标签提示词"
      }
    ]
  },
  "edit_workflows": {
    "type": "template_list",
    "description": "独立编辑工作流路由",
    "hint": "编辑工作流独立于生图家族。每条记录为 LLM 提供一个 edit_workflow 名称，并选择本地导入的工作流；也可在 Workflow Studio 的工作流页创建和绑定。",
    "obvious_hint": true,
    "templates": {
      "edit_workflow": {
        "name": "编辑工作流",
        "items": {
          "name": {
            "type": "string",
            "description": "编辑路由名",
            "default": "",
            "hint": "暴露给 LLM 的独立编辑路由名，例如 qwen-edit；无需在生图家族中创建同名项"
          },
          "workflow": {
            "type": "string",
            "description": "编辑工作流",
            "default": "",
            "hint": "从本地已导入工作流中选择；需包含来源图片 LoadImage 和编辑提示词节点"
          },
          "description": {
            "type": "string",
            "description": "路由说明",
            "default": "",
            "hint": "说明这条编辑工作流适合哪些编辑任务，帮助 LLM 在多个路由中选择"
          }
        }
      }
    },
    "default": []
  },
  "edit_families": {
    "type": "template_list",
    "description": "旧版编辑图家族配置（兼容）",
    "invisible": true,
    "hint": "旧版配置兼容项。新配置请使用独立编辑工作流路由。",
    "obvious_hint": true,
    "templates": {
      "edit_family": {
        "name": "编辑图家族",
        "items": {
          "model_family": {
            "type": "string",
            "description": "对应生图家族",
            "default": "",
            "hint": "选择上方已配置的家族，例如 qwen；新添加生图家族后先保存并重载插件，才能在这里的下拉框中选择"
          },
          "workflow": {
            "type": "string",
            "description": "编辑图工作流",
            "default": "",
            "hint": "选择已导入的图片编辑工作流；应包含 LoadImage 与编辑提示词节点"
          }
        }
      }
    },
    "default": []
  },
  "workflow_node_mappings": {
    "type": "template_list",
    "description": "工作流节点映射（配置优先）",
    "hint": "按工作流手动指定机器人可以修改的节点。节点填写纯 ID（如 478）或“ID — 节点类型 — 标题”。只要添加了该工作流条目，留空槽位就保持未映射；配置优先于 Workflow Studio 档案和自动检测，修改后重载插件生效。",
    "obvious_hint": true,
    "templates": {
      "mapping": {
        "name": "工作流节点映射",
        "items": {
          "workflow": {
            "type": "string",
            "default": "",
            "hint": "工作流模板名，须与模型家族中的 workflow 完全一致"
          },
          "prompt": {
            "type": "string",
            "default": "",
            "hint": "主提示词节点 ID（必填）"
          },
          "source_image": {
            "type": "string",
            "default": "",
            "hint": "编辑来源图片的 LoadImage 节点 ID；使用编辑工作流时必填"
          },
          "source_images": {
            "type": "string",
            "default": "",
            "hint": "多图编辑时按顺序填写 LoadImage 节点 ID，以逗号分隔；顺序对应工作流 images.image_1、images.image_2 等输入"
          },
          "resolution": {
            "type": "string",
            "default": "",
            "hint": "可选编辑 resolution 输入节点 ID；0 表示保留参考图尺寸"
          },
          "custom_size": {
            "type": "string",
            "default": "",
            "hint": "可选编辑 custom_size 开关节点 ID；启用后由工作流使用分辨率选择器画布"
          },
          "model": {
            "type": "string",
            "default": "",
            "hint": "底模加载节点 ID（允许机器人换底模时填写）"
          },
          "loras": {
            "type": "string",
            "default": "",
            "hint": "可选 LoRA 节点 ID；Anima 应映射 Power Loader，不要映射独立加速节点"
          },
          "size": {
            "type": "string",
            "default": "",
            "hint": "画面宽高节点 ID"
          },
          "aspect_ratio": {
            "type": "string",
            "default": "",
            "hint": "T2I 分辨率选择器的 aspect_ratio 输入节点 ID"
          },
          "megapixels": {
            "type": "string",
            "default": "",
            "hint": "T2I 分辨率选择器的 MP 目标输入节点 ID"
          },
          "sampler": {
            "type": "string",
            "default": "",
            "hint": "主采样节点 ID（必填）"
          },
          "sampler_2": {
            "type": "string",
            "default": "",
            "hint": "双采样第二段节点 ID；留空时保持第二段独立参数"
          },
          "negative": {
            "type": "string",
            "default": "",
            "hint": "负向提示词节点 ID"
          },
          "artist": {
            "type": "string",
            "default": "",
            "hint": "画师风格节点 ID"
          },
          "quality": {
            "type": "string",
            "default": "",
            "hint": "画质词节点 ID"
          },
          "trigger_words": {
            "type": "string",
            "default": "",
            "hint": "LoRA 触发词节点 ID"
          },
          "clip": {
            "type": "string",
            "default": "",
            "hint": "CLIP 加载节点 ID"
          },
          "vae": {
            "type": "string",
            "default": "",
            "hint": "VAE 加载节点 ID"
          },
          "guidance": {
            "type": "string",
            "default": "",
            "hint": "FluxGuidance 等引导强度节点 ID"
          }
        }
      }
    },
    "default": []
  },
  "default_recipe": {
    "type": "string",
    "default": "默认",
    "hint": "用户只说「画一张」时 comfyui_recipe_draw / comfyui_generate 用的配方名；在 Workflow Studio 静态配方列表点「设为默认」自动写入，也可直接填配方名"
  },
  "_tool_section": {
    "description": "=== LLM 工具 ===",
    "type": "text",
    "invisible": true
  },
  "llm_tool_mode": {
    "type": "string",
    "default": "basic",
    "options": [
      "basic",
      "full"
    ],
    "labels": [
      "基础（画图 + 查词）",
      "完整（开放诊断工具）"
    ],
    "hint": "basic 暴露 comfyui_draw / comfyui_recipe_draw / comfyui_lookup；配置独立编辑工作流后还会暴露 comfyui_edit。full 额外开放队列、任务、模型信息等诊断工具，其中高风险工具仍需打开下方开关"
  },
  "allow_llm_unsafe_tools": {
    "type": "bool",
    "default": false,
    "hint": "允许 LLM 执行任意工作流 JSON、上传本地文件到 ComfyUI、释放显存和删除配方。存在误操作与数据丢失风险，默认关闭；确有需要再开"
  },
  "node_slots": {
    "type": "object",
    "invisible": true,
    "description": "告诉插件：工作流里哪个格子写用户的话、哪个出图、哪个换底模。选好后机器人就不会填错。导入工作流后选项会刷新，重载插件生效。不会用的格子留空即可。",
    "items": {
      "prompt": {
        "type": "string",
        "default": "",
        "hint": "用户要画的内容写到哪（必选）"
      },
      "model": {
        "type": "string",
        "default": "",
        "hint": "底模写到哪（想让机器人换模型就选）"
      },
      "loras": {
        "type": "string",
        "default": "",
        "hint": "LoRA 写到哪"
      },
      "size": {
        "type": "string",
        "default": "",
        "hint": "画面宽高写到哪"
      },
      "sampler": {
        "type": "string",
        "default": "",
        "hint": "出图采样写到哪（步数/精细程度，必选）。双采样时选第一段"
      },
      "sampler_2": {
        "type": "string",
        "default": "",
        "hint": "双采样第二段写到哪（可留空；映射后才同步步数和种子，共享外联节点会自然联动）"
      },
      "negative": {
        "type": "string",
        "default": "",
        "hint": "不要出现的东西写到哪（可留空）"
      },
      "artist": {
        "type": "string",
        "default": "",
        "hint": "画师风格写到哪（可留空）"
      },
      "quality": {
        "type": "string",
        "default": "",
        "hint": "画质词写到哪（可留空）"
      },
      "trigger_words": {
        "type": "string",
        "default": "",
        "hint": "LoRA 触发词写到哪（可留空）"
      },
      "clip": {
        "type": "string",
        "default": "",
        "hint": "换文本编码器（Flux/Krea/Qwen 等独立 CLIP 的模型才需要，可留空）"
      },
      "vae": {
        "type": "string",
        "default": "",
        "hint": "换 VAE（模型用独立 VAE 时才需要，可留空）"
      },
      "guidance": {
        "type": "string",
        "default": "",
        "hint": "FluxGuidance 之类的引导值节点（可留空）"
      }
    }
  },
  "_lookup_section": {
    "description": "=== 查询源 ===",
    "type": "text",
    "invisible": true
  },
  "danbooru_base_url": {
    "type": "string",
    "default": "https://danbooru.donmai.us",
    "obvious_hint": true,
    "description": "danbooru API 文档：https://danbooru.donmai.us/wiki_pages/help:api",
    "hint": "danbooru 接口地址，国内可换可用镜像；官方 API 文档 https://danbooru.donmai.us/wiki_pages/help:api（可点击链接见 Workflow Studio 侧栏「相关链接」）"
  },
  "gelbooru_base_url": {
    "type": "string",
    "default": "https://gelbooru.com",
    "obvious_hint": true,
    "description": "gelbooru DAPI 文档：https://gelbooru.com/index.php?page=help&topic=dapi",
    "hint": "gelbooru 接口地址（镜像可换）；DAPI 文档 https://gelbooru.com/index.php?page=help&topic=dapi（可点击链接见 Workflow Studio 侧栏）"
  },
  "civitai_api_key": {
    "type": "string",
    "default": "",
    "obvious_hint": true,
    "description": "API Key 获取：https://civitai.com/user/account（镜像站路径相同）",
    "hint": "civitai API Key（可选，以你的身份调用）：获取 https://civitai.com/user/account，镜像站路径相同；API 参考 https://github.com/civitai/civitai/wiki/REST-API-Reference（可点击链接见 Workflow Studio 侧栏）"
  },
  "_legacy_defaults_section": {
    "description": "=== 旧版兼容默认值（有配方后以配方为准） ===",
    "type": "text",
    "invisible": true
  },
  "default_artist": {
    "type": "string",
    "invisible": true,
    "default": "",
    "hint": "默认画师串（如 (@名字:1.2)），留空用模板原值；仅 comfyui_generate 未传 artist 时生效"
  },
  "default_quality": {
    "type": "string",
    "invisible": true,
    "default": "",
    "hint": "默认质量词（如 masterpiece, best quality, score_9），留空用模板原值"
  },
  "default_trigger_words": {
    "type": "string",
    "invisible": true,
    "default": "",
    "hint": "默认 lora 触发词（如 @f1f,@deadpuritystyle），留空用模板原值"
  },
  "default_negative_prompt": {
    "type": "string",
    "invisible": true,
    "default": "",
    "hint": "默认负向提示词，留空用模板原值"
  },
  "default_model": {
    "type": "string",
    "invisible": true,
    "default": "",
    "hint": "没有配方时的兜底底模。有配方后以配方为准"
  },
  "default_lora": {
    "type": "template_list",
    "invisible": true,
    "description": "默认 LoRA 列表：可添加多条，按顺序映射 Power Lora Loader 插槽 lora_1..lora_N；每条含文件名与权重",
    "templates": {
      "lora": {
        "name": "LoRA",
        "items": {
          "name": {
            "type": "string",
            "default": "",
            "hint": "LoRA 文件名，需与 ComfyUI models/loras 下的相对路径一致"
          },
          "strength": {
            "type": "float",
            "default": 0.8,
            "slider": {
              "min": 0,
              "max": 1.5,
              "step": 0.05
            },
            "hint": "权重，常用 0.5~1.0"
          }
        }
      }
    },
    "default": []
  },
  "default_steps": {
    "type": "int",
    "invisible": true,
    "default": 0,
    "slider": {
      "min": 0,
      "max": 100,
      "step": 1
    },
    "hint": "默认采样步数，0=用模板原值"
  },
  "default_cfg": {
    "type": "float",
    "invisible": true,
    "default": 0,
    "slider": {
      "min": 0,
      "max": 20,
      "step": 0.5
    },
    "hint": "默认 CFG，0=用模板原值"
  },
  "default_sampler_name": {
    "type": "string",
    "invisible": true,
    "default": "",
    "options": [
      "",
      "er_sde",
      "euler",
      "euler_ancestral",
      "dpmpp_2m",
      "dpmpp_2m_sde",
      "dpmpp_3m_sde",
      "dpmpp_sde",
      "ddim",
      "uni_pc",
      "lcm"
    ],
    "hint": "默认采样器，留空=用模板原值"
  },
  "default_scheduler": {
    "type": "string",
    "invisible": true,
    "default": "",
    "options": [
      "",
      "normal",
      "karras",
      "exponential",
      "sgm_uniform",
      "simple",
      "beta"
    ],
    "hint": "默认调度器，留空=用模板原值"
  },
  "default_denoise": {
    "type": "float",
    "invisible": true,
    "default": 0,
    "slider": {
      "min": 0,
      "max": 1,
      "step": 0.05
    },
    "hint": "默认降噪强度 0~1，0=用模板原值"
  },
  "default_width": {
    "type": "int",
    "invisible": true,
    "default": 0,
    "hint": "默认图片宽度，0=用模板原值"
  },
  "default_height": {
    "type": "int",
    "invisible": true,
    "default": 0,
    "hint": "默认图片高度，0=用模板原值"
  },
  "default_workflow": {
    "type": "string",
    "default": "anima-v3",
    "hint": "旧版兼容项；model_families 为空时作为兜底工作流",
    "invisible": true
  },
  "_animadex_section": {
    "description": "=== AnimaDex 角色库 ===",
    "type": "text",
    "invisible": true
  },
  "animadex_mcp_url": {
    "description": "AnimaDex MCP 服务地址",
    "type": "string",
    "default": "http://127.0.0.1:11451/mcp",
    "hint": "本地角色库 MCP 服务端点；不使用 AnimaDex 查询可忽略"
  },
  "animadex_timeout": {
    "description": "AnimaDex 查询超时（秒）",
    "type": "float",
    "default": 8.0,
    "slider": {
      "min": 1,
      "max": 60,
      "step": 1
    },
    "hint": "单次角色库查询的等待上限，超时自动放弃并回退其他查询源"
  },
  "image_cache_auto_clean": {
    "type": "bool",
    "default": true,
    "description": "自动清理图片缓存",
    "hint": "启动及每小时按保留天数和容量清理 plugin_data 下的 output 图片；最近 5 分钟文件保留以供发送。"
  },
  "image_cache_days": {
    "type": "int",
    "default": 7,
    "description": "图片缓存保留天数",
    "hint": "0 关闭按时间清理。仅影响返回图片，不清理配方、工作流和历史记录。"
  },
  "image_cache_max_mb": {
    "type": "int",
    "default": 1024,
    "description": "图片缓存容量上限（MiB）",
    "hint": "0 不限制容量；超限时优先清理最久未使用的图片。近期保护文件可能暂时超限。手动 expired 也遵循此配置。"
  },
  "resource_family_rules": {
    "type": "template_list",
    "description": "资源模型家族归类规则",
    "hint": "用于底模与 LoRA 查询和兼容性校验。优先级高于元数据、目录/文件名推断；多个匹配时首条生效。家族名应与生图家族一致。",
    "templates": {
      "rule": {
        "name": "资源归类",
        "items": {
          "family": {
            "type": "string",
            "default": "anima",
            "hint": "家族名，例如 anima、krea2、sdxl 或自定义家族"
          },
          "kind": {
            "type": "string",
            "default": "all",
            "options": [
              "all",
              "model",
              "lora"
            ],
            "hint": "all=底模和LoRA，model=底模，lora=LoRA"
          },
          "pattern": {
            "type": "string",
            "default": "Anima/*",
            "hint": "匹配 ComfyUI 返回的完整相对文件名，支持 * 和 ?；忽略大小写与斜杠方向。例如 Anima/* 或 my_model.safetensors"
          }
        }
      }
    },
    "default": []
  }
}
